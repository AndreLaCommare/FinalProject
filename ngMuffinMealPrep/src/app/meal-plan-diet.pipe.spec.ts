import { MealPlanDietPipe } from './meal-plan-diet.pipe';

describe('MealPlanDietPipe', () => {
  it('create an instance', () => {
    const pipe = new MealPlanDietPipe();
    expect(pipe).toBeTruthy();
  });
});
